"""aix.retrieval — GraphRAG retrieval layer.

Modules:
    graph_retriever              — Hybrid Node2Vec + semantic retriever
    context_builder              — Educational context aggregation + ranking
    text2cypher                  — Natural-language → Cypher translation
    multilingual_text2cypher     — Multilingual wrapper around text2cypher
    query_metrics                — Per-query timing + observability
"""
