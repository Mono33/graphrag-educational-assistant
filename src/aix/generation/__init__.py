"""aix.generation — LLM response generation.

Modules:
    llm_chain — `EducationalResponseGenerator` (LangChain wrapper around the
                LLM that consumes structured `EducationalContext` and emits
                final responses).
"""
