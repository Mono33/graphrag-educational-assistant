"""Pytest configuration for graphaixlearning tests.

With ``pip install -e .`` (Phase 3A), all project modules are importable
without ``sys.path`` manipulation. This file now serves as a marker for
pytest and a place to add shared fixtures in the future.
"""
