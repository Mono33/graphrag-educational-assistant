# Marker file: makes ``scripts`` a Python package so utilities can be invoked
# as ``python -m scripts.<subdir>.<module>`` from the repo root, which puts
# the repo root on sys.path automatically (so ``import config`` etc. work).
